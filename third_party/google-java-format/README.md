This directory is required for `git cl format` to format Java code.

The command looks for a `google-java-format.jar` file, and then runs the
`google-java-format` script.

The `google-java-format.jar` in this directory is just a placeholder, since the
real on lives in `//third_party/google/...`
